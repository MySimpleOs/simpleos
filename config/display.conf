# Copy or mirror of rootfs/etc/display.conf (packaged into initrd).
# width/height are injected into Limine at `make iso`; refresh_hz/label are
# kernel-only. Keep in sync with rootfs if you edit this copy.
#
width=1920
height=1080
refresh_hz=60
label=primary
