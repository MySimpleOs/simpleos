# Mirror rootfs/etc/display.conf for initrd + Limine. Keep in sync.
# pointer=auto|ps2|virtio|usb — see rootfs/etc/display.conf for bare-metal notes.
width=1920
height=1080
refresh_hz=60
vsync=1
pointer=auto
label=primary
