# Display policy (initrd: /etc/display.conf). width/height → Limine `resolution:`
# at `make iso` (refresh_hz/label are kernel-only). Edit, rebuild ISO, reboot.
# 1920×1080 is a common VirtualBox GOP mode; set your panel size if different.
width=1920
height=1080
refresh_hz=60
label=primary
