# Display + pointer policy (initrd /etc/display.conf). Limine resolution from
# width/height at `make iso`. pointer=auto tries virtio-tablet-pci (QEMU),
# then PS/2. Use pointer=ps2 on VirtualBox without virtio tablet.
width=1920
height=1080
refresh_hz=60
pointer=auto
label=primary
