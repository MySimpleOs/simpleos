#!/usr/bin/env bash
# Faz 1 — build binutils + gcc cross-compiler for x86_64-elf.
# Installs into $SIMPLEOS_ROOT/toolchain/out. Stub for now.
set -euo pipefail

echo "scripts/build-toolchain.sh: not implemented yet (Faz 1)" >&2
exit 1
